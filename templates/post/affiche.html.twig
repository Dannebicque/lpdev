{% extends 'base.html.twig' %}

{% block body %}
<h1>Post</h1>
    <h2>{{ post.title }}</h2>
    <p>{{ post.content }}</p>
    <p>{{ post.dateCreated|date('d/m/Y') }}</p>

    <h1>Catégorie</h1>
    <h2>{{ category.title }}</h2>
{% endblock %}

{% block title %}

{% endblock %}

